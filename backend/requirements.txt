# Core (required to run the API + LangGraph orchestration)
pydantic>=2.6
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0
langgraph>=0.2.0

# LLM SDKs — install only the provider(s) you use (all optional)
anthropic>=0.39        # ANTHROPIC_API_KEY
openai>=1.40           # OPENAI_API_KEY / GROQ / OPENROUTER / AZURE / OLLAMA (OpenAI-compatible)
# Gemini needs NO SDK — it is called via REST. (google-generativeai not required)

# Dev
pytest>=8.0
pytest-asyncio>=0.23
